<div class="grid md:grid-cols-2 bg-white shadow-2xl rounded-2xl overflow-hidden max-w-6xl mx-auto">
  <!-- Left Side: Branding -->
  <div class="bg-blue-600 text-white p-10 hidden md:flex flex-col justify-between">
    <div>
      <h1 class="text-4xl font-bold mb-4 flex items-center">
        <span class="w-10 h-10 bg-white/20 rounded-xl inline-block mr-3"></span>
        CampusFlow
      </h1>
      <p class="text-lg font-light">
        Your campus marketplace for buying & selling everything students need.
      </p>
    </div>
    <div class="mt-10 space-y-4">
      <p class="flex items-center gap-3 text-white/90">📚 Books, notes & materials</p>
      <p class="flex items-center gap-3 text-white/90">💻 Electronics & gadgets</p>
      <p class="flex items-center gap-3 text-white/90">🎒 Campus essentials</p>
      <p class="flex items-center gap-3 text-white/90">🔒 Verified students only</p>
    </div>
    <p class="text-sm text-white/70 mt-10">Trusted by 10,000+ students</p>
  </div>

  <!-- Right Side: Login Form -->
  <div class="p-10 flex flex-col justify-center w-full">
    <h2 class="text-3xl font-bold mb-2">Welcome back</h2>
    <p class="text-gray-500 mb-8">
      New to CampusFlow?
      <a href="/register" class="text-blue-600 font-semibold hover:underline">Create account</a>
    </p>

    <form action="/login" method="POST" class="space-y-5">
      <div>
        <label for="username" class="block mb-1 text-sm font-medium">Username</label>
        <input
          type="text"
          name="username"
          id="username"
          required
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="Enter your username"
        />
      </div>

      <div>
        <label for="password" class="block mb-1 text-sm font-medium">Password</label>
        <input
          type="password"
          name="password"
          id="password"
          required
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          placeholder="••••••••"
        />
      </div>

      <div class="flex items-center justify-between">
        <label class="flex items-center text-sm">
          <input type="checkbox" name="remember" class="mr-2" />
          Remember me
        </label>
        <a href="/forgot" class="text-sm text-blue-600 hover:underline">Forgot password?</a>
      </div>

      <button
        type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-lg font-semibold transition"
      >
        Sign in
      </button>
    </form>

    <div class="my-6 flex items-center gap-4">
      <hr class="flex-grow border-gray-300" />
      <span class="text-gray-400 text-sm">or</span>
      <hr class="flex-grow border-gray-300" />
    </div>

    <div class="grid grid-cols-2 gap-4">
      <button
        type="button"
        class="flex items-center justify-center gap-2 border border-gray-300 py-2 rounded-lg hover:bg-gray-100 transition"
      >
        <img src="https://www.svgrepo.com/show/355037/google.svg" alt="Google" class="w-5 h-5" />
        Google
      </button>
      <button
        type="button"
        class="flex items-center justify-center gap-2 border border-gray-300 py-2 rounded-lg hover:bg-gray-100 transition"
      >
        📱 Mobile Number
      </button>
    </div>

    <p class="text-center text-sm text-gray-500 mt-8">
      Don't have an account?
      <a href="/register" class="text-blue-600 font-medium hover:underline">Sign up</a>
    </p>
  </div>
</div>
